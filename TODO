- smtpfront:

  - Implement a session timeout in addition to the I/O timeout.

  - Handle $LOCALIPHOST (all back-ends): rewrite envelope recipient
    addresses of the form box@[a.b.c.d], where a.b.c.d is a local IP
    address, to box@$LOCALIPHOST.

  - Add RFC 1893/2034 enhanced status codes to all responses.

  - Add support for RFC 2487 STARTTLS

  - Segment the SMTP code into seperate files

- pop3front-maildir:

  - Handle the TOP command

  - Properly re-parse flags in cmd_quit

- Write a IMAP authenticator? imapfront-auth (to be used intially as a
  front-end for Courier IMAP)
